[package]
name = "tmux-claude-state"
version = "0.1.1"
edition = "2024"
description = "Claude session state detection and tmux helpers"
license = "MIT"
repository = "https://github.com/maedana/claudeye"

[dependencies]
regex = "1"
