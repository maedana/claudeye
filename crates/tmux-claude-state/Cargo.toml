[package]
name = "tmux-claude-state"
version = "0.1.3"
edition = "2024"
description = "Claude session state detection and tmux helpers"
license = "MIT"
repository = "https://github.com/maedana/claudeye"
keywords = ["tmux", "claude", "session", "state", "monitor"]
categories = ["development-tools::testing", "api-bindings"]

[dependencies]
regex = "1"
